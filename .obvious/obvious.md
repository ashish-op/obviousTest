# obviousTest — Agent Guide

## Repository Status

**This repository is empty.** It has no source files, no dependency manifests
(package.json, go.mod, pyproject.toml, Gemfile, etc.), no Dockerfile or Compose
file, no Makefile, and no docs. There is no application here yet — nothing to
install, build, run, test, or map.

This guide was generated by an automated onboarding run on 2026-09-17 for the
empty-repository case. It deliberately makes **no stack, command, or port
claims**, because none could be verified against real code.

## Stack

Not determined yet. TODO(next setup run): detect and record the actual
runtime(s), package manager, and services once code lands in this repository.

## Commands

None. There is no package-manager manifest, Makefile, or entry point in this
repository, so no commands exist to document. Do not guess any.

TODO(next setup run): record the verified local dev commands here
(install, dev/start, test, lint/typecheck), confirmed by actually running them.

## Codebase Map

Not applicable — the repository contains no files to map.
`.obvious/codebase-map.md` was deliberately omitted by this run and will be
generated by the next setup run once the repository has code.

## Local Verification

Not applicable — there is no local dev stack to verify, so no Validation
Summary is recorded. The next setup run will bring the stack up, exercise a
primary user flow, and record verified evidence here.

## Sandbox Snapshot

None captured. The snapshot phase is only reachable when the dev stack is
healthy; an empty repository has no dev stack and cannot pass that gate.

TODO(next setup run): capture the sandbox snapshot and record its ID and
ISO-8601 timestamp here.
